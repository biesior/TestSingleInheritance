Sample usage of inherited tables
=================================

 - Play ver.: 2.3.x
 - Database models are placed in `models.db` package
 - `News`, `Video` and `Post` models extends `Content`
 - all types uses a `content` table with `dtype` discriminator
